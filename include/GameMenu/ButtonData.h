#pragma once
#include <SFML/Graphics.hpp>

namespace ButtonData {

    const sf::Vector2f SIZE_BUTTON = { 200.f, 80.f }; // גודל כפתור

    const sf::Vector2f LOC_START = { 0.f, 0.f }; // מיקום כפתור התחלה
    const sf::Vector2f LOC_EXIT = { 0.f, 160.f }; // מיקום כפתור יציאה
    const sf::Vector2f LOC_HELP = { 0.f, 320.f }; // מיקום כפתור עזרה
    const sf::Vector2f LOC_STORE = { 0.f, 480.f }; // מיקום כפתור חנות

}
